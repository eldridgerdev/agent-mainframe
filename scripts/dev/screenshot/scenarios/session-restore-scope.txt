# session-restore-scope: reproduction setup for GitHub issue #633
# (restoring a picked Claude session for one named session in a feature was
# overwriting and relaunching every Claude-kind session in that feature with
# the same id). Shows the feature ending up with two independent Claude-kind
# sessions and the saved-session picker entry point opened from just one of
# them, scoped by the currently selected session row.
#
# This is a UI-only capture: no real `claude` process is launched.
#   - --config session-restore-scope-config.json sets an unreachable
#     low_memory_warn_mb so the pre-start resource gate trips on the very
#     first automation-seeded feature, which `create_feature_from_request`
#     treats as "create but leave stopped" -- the same skip-with-warning
#     path agent-limits.txt exercises deliberately. Without this,
#     --seed-feature autostarts the first agent for real (see that
#     scenario's own comment).
#   - The 's' add-session picker is never driven, because adding a session
#     to a feature that is not yet running brings its tmux session up and
#     launches the real harness for the session just added. Instead, a run:
#     step clones the seeded feature's sole Claude session directly as a row
#     in the scratch instance's own amf.db (feature_sessions table -- AMF
#     persists to SQLite, not a JSON file), then restarts the isolated pane
#     so AMF reloads it from disk -- the same "seed on disk, then respawn"
#     trick other scenarios use to pick up automation-written state.
#
# The fix itself (which session's claude_session_id gets overwritten on a
# real restore) is not something the TUI displays for a human to compare on
# screen -- AMF never shows a session's raw resume id. That correctness is
# proven by the automated regression test added alongside the fix
# (restore_claude_session_only_affects_the_targeted_session in
# src/app/tests/feature_sessions.rs), not by this screenshot. This capture
# only proves the reproduction shape from the issue is real, and that the
# picker's entry point is scoped to whichever session row is selected.
#
# Requires:
#   --config session-restore-scope-config.json
#   --seed session-restore-scope-project.json
#   --seed-feature session-restore-scope-feature.json
# (agent=claude, use_worktree=false, so the feature's workdir is the
# checked-out repo root -- no git worktree needed).

note:The seeded feature is created but deliberately left stopped (the resource gate's skip-with-warning path, forced by the config's unreachable memory floor) -- no real agent process starts during seeding. It starts with exactly one Claude-kind session, shown by the "[1]" badge on the still-collapsed feature row.
expect:session-restore-scope-demo
expect:restore-me
expect:[1]
shot:feature-before-second-session

run:sqlite3 "$XDG_CONFIG_HOME/amf/amf.db" "INSERT INTO feature_sessions (id, feature_id, kind, label, tmux_window, claude_session_id, token_usage_source, token_usage_source_match, created_at, command, on_stop, pre_check, sort_order, todo_id, todo_launched_from_menu) SELECT lower(hex(randomblob(16))), feature_id, kind, 'Claude 2', 'claude-2', claude_session_id, token_usage_source, token_usage_source_match, created_at, command, on_stop, pre_check, sort_order + 1, todo_id, todo_launched_from_menu FROM feature_sessions WHERE kind = 'claude' LIMIT 1;"
wait:300
run:tmux respawn-pane -k -t "$SESSION" "$AMF_BIN"
wait:1500

key:j
wait:300
note:After the pane reloads the store from disk, the feature's badge reads "[2]" -- a second Claude-kind session now exists in the same feature, reproducing the exact starting shape from issue #633 (two Claude-kind sessions, distinct tmux windows, in one feature). Still collapsed, so the individual session rows are not shown yet.
expect:restore-me
expect:[2]
shot:feature-now-has-two-sessions

key:Enter
wait:300
note:Expanding the feature row reveals both Claude-kind sessions as independent rows -- "Claude 1" and "Claude 2".
expect:Claude 1
expect:Claude 2
shot:two-claude-sessions

key:j
wait:200
note:The cursor now sits on "Claude 1" specifically, not "Claude 2" -- this is the session a restore must be scoped to. Before the fix, confirming a picked session from here would also overwrite and relaunch "Claude 2" with the same id.
expect:Claude 1
shot:claude-1-selected

key:S
wait:400
note:"S" opens the saved-transcript picker for the selected session, "Claude 1". The project seed points at this checked-out repo, so real Claude transcript history for this worktree is listed. Which session's stored id actually changes on a confirmed restore is verified by the regression test, not visible as a raw id here. This capture cancels rather than confirming, so nothing is relaunched.
expect:Claude Sessions
shot:saved-session-picker-from-claude-1

key:Escape
wait:200
note:Cancelling the picker returns to the dashboard unchanged -- "Claude 1" is still the selected row, "Claude 2" is untouched, and nothing was relaunched.
expect:Claude 1
expect:Claude 2
shot:picker-cancelled
