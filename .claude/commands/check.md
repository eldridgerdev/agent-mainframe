Run `cargo check` to type-check the project without a full
build. This is faster than a full build and catches all type
errors.

If there are errors, analyze and fix them, then re-run
`cargo check` to verify.
