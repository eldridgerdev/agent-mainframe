Run `cargo clippy -- -W clippy::all` to lint the project.
If there are warnings or errors, fix them and re-run to
verify a clean lint pass.

Focus on fixing the actual issues, not suppressing warnings
with `#[allow(...)]` unless the warning is a false positive.
