# PR review fixture

This file exists only on the `test/pr-review-fixture` branch. It is a
throwaway change for manually testing AMF's "Review a PR" mode. Do not merge.

- An added file (this one).
- A modified file with two hunks (`src/diff.rs`).
- A renamed file with an edit (`docs/usability-review.md`).
