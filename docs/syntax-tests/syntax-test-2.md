# Syntax Fixture Two

This markdown file exists to test:

- headings
- emphasis like **bold** and _italic_
- inline code such as `cargo check`

```rust
fn main() {
    println!("markdown fixture");
}
```
